<div class="elemental-css">
    {{#if title}}<div class="{{theme.roll_title}}">
        {{title}}
    </div>{{/if}}
    <div class="twel:mb-2">
        {{#each badges }}
            <span class="{{../theme.modifier_badge}}">{{this}}</span>
        {{/each}}
    </div>
    {{# if exploded }}
        {{# if is_damage_roll }}
            <div class="{{theme.warning_badge}}">{{localize "Elemental.Rolls.MassiveDamage"}}</div>
        {{/if}}
    {{/if}}
    <div class="dice-roll">
        {{#if flavor}}
        <div class="dice-flavor">{{flavor}}</div>
        {{/if}}
        <div class="dice-result">
            <div class="dice-formula">{{formula}}</div>
            {{{tooltip}}}
            <h4 class="dice-total">{{total}}</h4>
        </div>
    </div>
    <div class="twel:mt-2 twel:flex twel:justify-between">
        {{# if is_damage_roll }}
            <button class="elemental-roll-damage {{theme.roll_button_left}}">
                <i class="fas fa-bolt"></i> {{localize "Elemental.ApplyDamage"}}
            </button>
            <button class="elemental-roll-damage-mod {{theme.roll_button_right}}">
                <i class="fas fa-plus"></i>
            </button>
        {{else}}
            {{#unless originating_roll }}
                {{#unless is_difficulty_roll}}
                    <button class="elemental-roll-difficulty {{theme.roll_button}}">
                        <i class="fas fa-dice"></i> {{localize "Elemental.DIFRoll"}}
                    </button>
                {{/unless}}
                <button class="elemental-roll-opposed {{theme.roll_button}}">
                    <i class="fas fa-dice"></i> {{localize "Elemental.OpposingRoll"}}
                </button>
            {{/unless}}
        {{/if}}
    </div>
    {{{result_div}}}
</div>